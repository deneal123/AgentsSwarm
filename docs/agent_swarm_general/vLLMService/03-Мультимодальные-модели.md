# vLLM Service — Мультимодальные модели

## Стратегия мультимодальных моделей

Платформа использует две мультимодальные модели с различными профилями:

- **LLaVA-1.6 (LLaVA-NeXT)** — полнофункциональная модель (7.5B параметров) для детального анализа изображений, требует GPU A100/A10G
- **SmolVLM2-2.2B-Instruct** — компактная модель (2.2B параметров) для эффективного анализа изображений и видео, работает на GPU L4 и выше

Выбор модели определяется сценарием: SmolVLM2 для оперативных задач и edge-развёртывания, LLaVA — для задач, требующих максимального качества анализа.

---

## SmolVLM2-2.2B-Instruct

### Характеристики

| Параметр | Значение |
|----------|---------|
| Разработчик | HuggingFace (`HuggingFaceTB/SmolVLM2-2.2B-Instruct`) |
| Параметры | 2.2 миллиарда |
| Контекстное окно | 8192 токена |
| Поддерживаемые модальности | Изображения, видео, vision chunks |
| Максимальный размер изображения | Longest edge 384 px |
| Точность | BF16 |
| GPU | NVIDIA L4 (24GB) и выше — протестирована на одном L4 |
| GPU Memory | ~6–8 GB (BF16) |
| Режим выполнения | Eager mode (enforce_eager) — оптимально для малых моделей |
| LoRA | Поддержка динамических LoRA-адаптеров |

### Преимущества SmolVLM2

- **Компактность**: в 3.4 раза меньше LLaVA (2.2B vs 7.5B), что позволяет работать на бюджетных GPU (L4 вместо A100)
- **Видео**: поддержка анализа видео с настраиваемым числом кадров (по умолчанию 16), чего не может LLaVA
- **Контекст**: окно 8192 токена — вдвое больше, чем у LLaVA (4096), позволяет передавать более подробные инструкции
- **LoRA-адаптация**: нативная поддержка LoRA-адаптеров для доменной специализации
- **Кэш мультимодального процессора**: встроенный кэш (настраиваемый, по умолчанию 4 ГБ) для ускорения обработки повторяющихся изображений

### Формат промптов

SmolVLM2 использует специфический формат промптов:

- Начало пользовательского сообщения: тег `<|im_start|>User:`
- Вставка изображения: тег `<image>` в позиции, где должно быть изображение
- Вопрос или инструкция: текст после тега изображения
- Завершение пользовательского сообщения: тег `<end_of_utterance>`
- Начало ответа: `Assistant:`

### Поддерживаемые модальности

| Модальность | Описание | Применение в платформе |
|------------|----------|----------------------|
| **image** | Одиночное изображение | Анализ кадров с камер роботов |
| **video** | Последовательность кадров с метаданными | Анализ видео для понимания действий и событий |
| **vision_chunk** | Фрагмент изображения с типизацией | Детальный анализ области интереса |

### Анализ видео

SmolVLM2 — единственная мультимодальная модель в платформе с поддержкой видео-анализа:

- Видео разбирается на N кадров (настраивается, по умолчанию 16)
- Кадры передаются в модель вместе с метаданными видео (длительность, FPS)
- Модель анализирует последовательность кадров и формирует описание происходящего
- Применение: анализ действий робота, обнаружение аномалий в поведении, описание событий в зоне наблюдения

### Параметры движка vLLM для SmolVLM2

| Параметр | Значение | Обоснование |
|----------|---------|-------------|
| `max_model_len` | 8192 | Полное использование контекстного окна |
| `max_num_seqs` | 2 | Ограничение параллельных запросов для стабильности на L4 |
| `enforce_eager` | True | Отключение CUDA Graph — оптимально для малых моделей |
| `mm_processor_kwargs` | max_image_size: longest_edge 384 | Контроль разрешения входных изображений |
| `limit_mm_per_prompt` | 1 (image) | Ограничение мультимодальных объектов на запрос |
| `mm_processor_cache_gb` | 4 | Размер кэша мультимодального процессора в ГБ |

### Batch Inference

SmolVLM2 поддерживает пакетную обработку запросов:

- Несколько промптов с изображениями обрабатываются одним вызовом
- Для повторяющихся изображений используется оптимизация через **UUID-идентификаторы**: каждому изображению присваивается UUID, и при повторном запросе с тем же UUID процессор использует кэшированный результат
- Вероятность повторного использования изображения настраивается для симуляции cache hit/miss — в production-окружении с роботами, отправляющими кадры одной сцены, hit rate высокий

---

## LLaVA-1.6 (LLaVA-NeXT)

### Характеристики

| Параметр | Значение |
|----------|---------|
| Архитектура | Vision Encoder (CLIP ViT-L/14) + LLM (Vicuna/Mistral 7B) |
| Параметры | ~7.5 миллиардов (суммарно) |
| Контекстное окно | 4096 токенов (текст) + визуальные токены |
| Разрешение изображений | До 672×672 (AnyRes — динамическое разрешение) |
| Точность | BF16 |
| GPU Memory | ~16 GB (BF16) |
| Поддерживаемые форматы | JPEG, PNG, BMP |

### Архитектура AnyRes

LLaVA-1.6 использует технологию AnyRes (Any Resolution), которая позволяет обрабатывать изображения произвольного разрешения:

1. Изображение разбивается на патчи фиксированного размера (336×336)
2. Каждый патч обрабатывается Vision Encoder независимо
3. Глобальное представление изображения (уменьшенное до 336×336) обрабатывается отдельно
4. Все визуальные токены (локальные и глобальные) конкатенируются
5. Визуальные токены подаются в LLM вместе с текстовыми

Это обеспечивает лучшее распознавание мелких деталей по сравнению с фиксированным ресайзом.

---

## Применение в платформе

### Выбор модели по задаче

| Задача | Рекомендуемая модель | Обоснование |
|--------|---------------------|-------------|
| Оперативный анализ кадров | SmolVLM2 | Низкая латентность, малый GPU footprint |
| Анализ видео с камер | SmolVLM2 | Единственная модель с поддержкой видео |
| Детальное описание сложной сцены | LLaVA-1.6 | Более высокое качество при 7.5B параметрах |
| Верификация детекций Triton | LLaVA-1.6 или SmolVLM2 | В зависимости от доступных GPU |
| Edge-развёртывание (GPU L4) | SmolVLM2 | Помещается на L4, LLaVA требует A10G+ |
| Чат с изображением от пользователя | SmolVLM2 | Быстрый отклик для интерактивных сценариев |

### Анализ сцены с камер роботов

Основной сценарий — понимание того, что видит робот, в контексте текстовой команды или вопроса:

- «Что ты видишь на складе?» → модель описывает сцену
- «Есть ли препятствия на пути к точке А?» → модель анализирует изображение с камеры
- «Опиши состояние объекта на столе» → детальное описание целевого объекта

### Верификация результатов Triton

Мультимодальная модель используется для проверки и интерпретации результатов компьютерного зрения:

- Triton обнаружил объект → LLaVA подтверждает/уточняет классификацию
- Сегментация показала аномалию → LLaVA даёт текстовое описание
- Неуверенная детекция (confidence < 0.5) → LLaVA помогает принять решение

### Ситуационная осведомлённость

Safety Agent использует мультимодальную модель для оценки безопасности:

- Оценка ситуации на изображении: наличие людей, опасных предметов, нестандартных условий
- Описание инцидентов для логирования: текстовое описание того, что произошло
- Принятие решений при неоднозначных ситуациях: «На пути предмет — это препятствие или целевой объект?»

### Поддержка пользователя

В чат-интерфейсе пользователь может отправить изображение с вопросом:

- Фото рабочей зоны: «Расставь роботов для уборки этой области»
- Скриншот с камеры: «Что здесь не так?»
- Схема помещения: «Спланируй маршрут обхода»

---

## Формат запросов

### Структура мультимодального запроса

Запрос к LLaVA передаётся в формате, совместимом с OpenAI Vision API:

1. **Роль system**: системный промпт с инструкциями
2. **Роль user**: текст + одно или несколько изображений
3. **Изображения**: передаются как base64 или URL (для MinIO)

### Ограничения

- Максимум 4 изображения в одном запросе (ограничение по GPU-памяти)
- Рекомендуемое разрешение: 672×672 (выше — автоматический ресайз)
- Формат: только растровые изображения (JPEG, PNG)

---

## Промпты для мультимодальных задач

### Промпт анализа сцены

Системный промпт определяет модель как анализатора робототехнических сцен. Включает: описание типов объектов, которые могут встречаться, формат ответа (структурированное описание: объекты, их расположение, состояние, риски), инструкции по оценке проходимости и безопасности.

### Промпт верификации

Для верификации детекций промпт содержит: результаты Triton (bounding boxes, классы, confidence), инструкцию подтвердить или скорректировать, формат ответа с полями: confirmed (boolean), corrected_class, description, confidence_assessment.

### Промпт безопасности

Специализированный промпт для Safety Agent: перечень опасных ситуаций, шкала рисков (1–10), обязательные поля ответа (risk_level, description, recommended_action, urgency).

---

## Взаимодействие с Triton

### Совместный пайплайн

В сложных сценариях Triton и LLaVA работают последовательно:

1. Изображение поступает от робота
2. Triton выполняет «Полный анализ сцены» (детекция, сегментация, глубина)
3. Результаты Triton + оригинальное изображение отправляются в LLaVA
4. LLaVA формирует высокоуровневое текстовое описание с учётом результатов CV
5. Объединённый результат (структурированные данные Triton + текстовое описание LLaVA) передаётся в Orchestrator

### Преимущества совместной работы

- Triton даёт точные координаты и метрики (bounding boxes, расстояния)
- LLaVA даёт семантическое понимание (контекст, намерения, оценка ситуации)
- Orchestrator получает полную картину: и числовые данные, и текстовую интерпретацию

---

## Оптимизация

### Visual Token Reduction (LLaVA)

Для снижения нагрузки на GPU и уменьшения латентности:

- Количество визуальных токенов можно сократить через pooling (average pooling патчей)
- Для простых запросов (общее описание) используется уменьшенное разрешение
- Для детальных запросов (анализ конкретного объекта) — полное разрешение с crop интересующей области

### Кэш мультимодального процессора (SmolVLM2)

SmolVLM2 в vLLM поддерживает выделенный кэш мультимодального процессора:

- Размер настраивается параметром `mm_processor_cache_gb` (по умолчанию 4 ГБ)
- Кэширует результаты пре-процессинга изображений и видео
- При повторных запросах к одному изображению (идентифицируется по UUID) пре-процессинг пропускается
- В робототехнических сценариях, где камеры часто наблюдают одну и ту же сцену, кэш значительно снижает латентность
- Кэш можно отключить (установить 0 ГБ) для экономии CPU-памяти при ограниченных ресурсах

### Кэширование визуальных эмбеддингов

При повторных запросах к одному и тому же изображению (например, разные вопросы к одному кадру) визуальные эмбеддинги кэшируются в Redis. Это позволяет при втором и последующих запросах выполнять только текстовый inference, что ускоряет ответ в 2–3 раза.

### LoRA-адаптеры для мультимодальных моделей

SmolVLM2 поддерживает динамическую загрузку LoRA-адаптеров, что позволяет:

- Адаптировать модель к специфике конкретного домена (складские объекты, медицинское оборудование, промышленные установки) без полного дообучения
- Использовать разные адаптеры для разных типов задач (детекция аномалий vs описание сцены)
- Загружать адаптеры per-request, обеспечивая гибкость в рамках одного экземпляра vLLM
- Комбинировать с квантизацией базовой модели для минимального GPU footprint

### Tensor Parallelism

Для моделей, не помещающихся на один GPU (или для увеличения throughput), vLLM поддерживает Tensor Parallelism. SmolVLM2 поддерживает параметр `tensor_parallel_size` для распределения модели по нескольким GPU. Для SmolVLM2 (2.2B) это редко необходимо, но может использоваться для LLaVA при ограниченной памяти одного GPU.

### Приоритизация запросов

| Приоритет | Тип запроса | Max Queue Wait |
|-----------|------------|----------------|
| Критический | Safety — оценка опасности | 100 мс |
| Высокий | Orchestrator — планирование | 500 мс |
| Средний | Верификация детекций | 2 сек |
| Низкий | Пользовательский чат | 5 сек |

---

## Мониторинг

### Метрики

| Метрика | Описание | Целевое значение |
|---------|----------|-----------------|
| ttft_multimodal_p95 | Time-to-First-Token (мультимод.) | < 1 сек (LLaVA), < 500 мс (SmolVLM2) |
| tokens_per_sec_multimodal | Скорость генерации | > 20 tok/s (LLaVA), > 30 tok/s (SmolVLM2) |
| image_encoding_time_p95 | Время кодирования изображения | < 200 мс |
| video_encoding_time_p95 | Время кодирования видео (SmolVLM2) | < 500 мс |
| multimodal_request_count | Число мультимод. запросов | Мониторинг |
| multimodal_error_rate | Доля ошибок | < 1% |
| gpu_memory_multimodal | Память GPU под мультимод. модель | < 18 GB (LLaVA), < 8 GB (SmolVLM2) |
| mm_processor_cache_hit_rate | Hit rate кэша мультимод. процессора | Мониторинг (SmolVLM2) |

### Типичные проблемы

| Проблема | Причина | Решение |
|----------|---------|---------|
| Высокая латентность | Большое разрешение изображения | Уменьшить разрешение или использовать crop |
| OOM при нескольких изображениях | Много визуальных токенов | Ограничить число изображений в запросе |
| Некорректное описание | Нечёткое изображение | Улучшить качество камеры или использовать Triton-результаты |
| Галлюцинации | Модель «дофантазирует» | Снизить temperature, добавить grounding через Triton |
