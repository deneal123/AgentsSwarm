import { colors } from "@theme/tokens";
import { PROJECT_NAME } from "@constants";

export const AUTH_FONT_FAMILY =
  "'Avenir Next', 'SF Pro Display', 'Manrope', 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif";

export const AUTH_THEME = {
  pageBg: "#060606",
  accent: "#ef4444",
  accentHover: "#dc2626",
  border: "rgba(255, 255, 255, 0.15)",
  mutedText: colors.text.secondary,
};

export const AUTH_BRAND_LABEL = PROJECT_NAME;
