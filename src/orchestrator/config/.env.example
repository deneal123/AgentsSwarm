ENV=dev
HOST=0.0.0.0
PORT=8000
RELOAD=0

# Redis backend for sessions (override per env)
REDIS_URL=redis://redis:6379/0

# MCP endpoints
MISSION_CONTROL_URL=http://mission-control:8050
MISSION_DISPATCH_URL=http://mission-dispatch:8051
ROS_MSP_COMMAND=uv
ROS_MSP_ARGS="--directory /opt/ros-mcp-server run server.py"
